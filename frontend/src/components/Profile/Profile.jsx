import { ProfileCard } from "./ProfileCard";

const Profile = () => {
  return (
    <section className="w-screen min-h-screen flex flex-col items-center font-marcellus mt-20">
      <ProfileCard />
    </section>
  );
};
export default Profile;
